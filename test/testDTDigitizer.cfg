process TEST = {
  source = PoolSource{ string fileName = "input.root" 
    untracked int32 maxEvents = 5
  }

  module dtDigitizer = DTDigitizer{ 
    bool onlyMuHits = false
    bool interpolate = true
    untracked bool debug = true
    double vPropWire = 24.4
    double deadTime = 150
    double smearing = 3.
  }

  module out = PoolOutputModule {
     	untracked string fileName ="rawdata.root"
  }
	 
  path p = {dump,out}
}
