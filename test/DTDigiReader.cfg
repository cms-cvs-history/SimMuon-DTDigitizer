process DTDigiReader = { 
  source = PoolSource{ 
	untracked vstring fileNames = {"file:digi.root"} 
        untracked int32 maxEvents = -1
  }
 
  module dtDigiReader = DTDigiReader{
	untracked string label = "muondtdigi"
	}

  path p = {dtDigiReader}
}
